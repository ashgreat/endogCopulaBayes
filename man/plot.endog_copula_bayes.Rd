% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostics.R
\name{plot.endog_copula_bayes}
\alias{plot.endog_copula_bayes}
\title{Plot a Bayesian Copula Regression Fit}
\usage{
\method{plot}{endog_copula_bayes}(x, params = NULL, which = c("trace", "density"), ...)
}
\arguments{
\item{x}{An object of class \code{endog_copula_bayes}.}

\item{params}{Character vector of parameter names to plot (after
variable-label substitution for a formula fit); \code{NULL} (default) plots
all named parameters in \code{x$parameters}.}

\item{which}{Character vector selecting which diagnostic plots to draw:
\code{"trace"} for traceplots, \code{"density"} for posterior density plots, or
both (the default).}

\item{...}{Ignored.}
}
\value{
\code{x}, invisibly.
}
\description{
Base-graphics traceplots and posterior density plots for the named
parameters of a fitted \code{endog_copula_bayes} object (the \code{2 * N} unnamed
Dirichlet probability masses are excluded).
}
\examples{
set.seed(1)
n <- 60
z <- rlnorm(n)
x <- rnorm(n)
dat <- data.frame(y = 2 - 4 * z + 6 * x + rnorm(n, sd = sqrt(2)),
                  z = z, x = x)
fit <- CopRegBayes(dat, iterations = 100, burnin = 20, thin = 2, seed = 42)
plot(fit, params = c("beta_z", "beta_x"))
}
\seealso{
\code{\link[=CopRegBayes]{CopRegBayes()}}, \code{\link[=diagnostics]{diagnostics()}}
}
