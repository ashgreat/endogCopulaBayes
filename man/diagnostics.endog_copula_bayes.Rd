% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostics.R
\name{diagnostics.endog_copula_bayes}
\alias{diagnostics.endog_copula_bayes}
\title{MCMC Convergence Diagnostics for a Bayesian Copula Regression Fit}
\usage{
\method{diagnostics}{endog_copula_bayes}(object, ...)
}
\arguments{
\item{object}{An object of class \code{endog_copula_bayes}.}

\item{...}{Ignored.}
}
\value{
A \code{data.frame} with one row per named parameter (row names give
the parameter names, respecting variable labels for a formula fit) and
columns:
\describe{
\item{\code{ess}}{Effective sample size.}
\item{\code{geweke_z}}{Geweke (1992) convergence z-score comparing the
first 10\% and last 50\% of the thinned posterior sample.}
\item{\code{n_draws}}{Number of thinned posterior draws used.}
}
}
\description{
Computes the effective sample size (ESS) and Geweke (1992) convergence
z-score for each of the named model parameters (the regression
coefficients, residual variance, copula correlations, and hyperprior
variances) using the thinned posterior sample stored in \code{object$posterior}.
The \code{2 * N} unnamed Dirichlet probability masses are not included.
}
\details{
Both statistics are implemented in base R using the same spectral-density
-at-zero estimator as \code{coda::spectrum0.ar()}: an AR(p) model is fit to
each parameter's draws with the order selected by AIC (via \code{\link[stats:ar]{stats::ar()}}),
and the resulting \code{var.pred / (1 - sum(ar))^2} is used as the long-run
variance in both the ESS ratio and the Geweke z-score denominator. A
column whose posterior draws are (numerically) constant has an
undefined spectral density; its ESS is reported as \code{0} and its Geweke
z-score as \code{NA}, rather than raising an error.
}
\examples{
set.seed(1)
n <- 60
z <- rlnorm(n)
x <- rnorm(n)
dat <- data.frame(y = 2 - 4 * z + 6 * x + rnorm(n, sd = sqrt(2)),
                  z = z, x = x)
fit <- CopRegBayes(dat, iterations = 100, burnin = 20, thin = 2, seed = 42)
diagnostics(fit)
}
\seealso{
\code{\link[=CopRegBayes]{CopRegBayes()}}, \code{\link[=plot.endog_copula_bayes]{plot.endog_copula_bayes()}}
}
