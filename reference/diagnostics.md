# MCMC Convergence Diagnostics

Generic function computing per-parameter MCMC convergence diagnostics.

## Usage

``` r
diagnostics(object, ...)
```

## Arguments

- object:

  A fitted model object.

- ...:

  Passed on to methods.

## Value

A `data.frame` of per-parameter diagnostics.
